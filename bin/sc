#!/bin/bash

cmd="bash -l"
opt=(
  -e \`\`
  -s $cmd
  -q
)

case $1 in
  -* )
    screen "$@"
    ;;
  '' )
    screen ${opt[@]}
    ;;
  * )
    screen ${opt[@]} -r "$1" || screen ${opt[@]} -S "$1"
    ;;
esac
