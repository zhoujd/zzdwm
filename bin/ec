#!/bin/sh

cmd=${cmd:-"emacsclient"}
hash $cmd 2>/dev/null
if [ $? -eq 0 ]; then
    if [ -z "$DISPLAY" ]; then
        opt="-t"
    else
        opt="-c"
    fi
    $cmd -n "$opt" "$@" 2>/dev/null || echo "No emacs daemon."
else
    echo "No $cmd found!"
fi
