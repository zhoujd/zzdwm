#!/bin/sh

cmd=emacsclient
hash $cmd 2>/dev/null
if [ $? -eq 0 ]; then
    if [ -z "$DISPLAY" ]; then
        opt=-t
    else
        opt=-c
    fi
    exec $cmd -n $opt "$@"
else
    echo "No $cmd found!"
fi
