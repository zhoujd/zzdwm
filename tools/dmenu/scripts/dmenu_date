#!/bin/bash

# If Sunday, show the week number of the next week
if [ $(date +'%u') -gt 6 ]; then
    WEEK=$(date +'W%-V' -d "next monday")
else
    WEEK=$(date +'W%-V')
fi
NOW=$(date +'%a %-e %b %-l:%M %p %Z %Y')
TM="W${WEEK} ${NOW}"
OPT=(
    -l 10
    -p date:
)
echo $TM | dmenu ${OPT[@]}
